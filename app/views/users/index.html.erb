<div class="container">
 <!--  <div class="alert alert-warning alert-dismissible fade show" role="alert">
  No more stays with horny men.
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div> -->
  <div class="quouchers"><p>Quouchers in <%= params[:search].capitalize %></p></div>
      <div class="masonry">
      <% @users.each do |user| %>
           <%= link_to user_path(user), class: "grid-user" do  %>
        <div class="grid-item">
           <%= image_tag user.profile_picture_url, alt: "alttext", class: "grid-item-picture"%>
           <div class="grid-item-infos">
            <p class="grid-item-title"><%= user.name %></p>
            <p class="grid-item-bio"><%= truncate(user.bio, length: 50) %></p>
          </div>
        </div>
        <% end %>
      <% end %>
    </div>
</div>

