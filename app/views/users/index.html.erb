<div class="container">
 <!--  <div class="alert alert-warning alert-dismissible fade show" role="alert">
  No more stays with horny men.
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
</div> -->
      <div class="masonry">
      <% @users.each do |user| %>
        <div class="grid-item">
           <%= image_tag user.profile_picture_url, alt: "alttext", class: "grid-item-picture"%>
           <div class="grid-item-infos">
            <span class="grid-item-title"><%= link_to user.name , user_path(user) %></span>
            <p class="grid-item-bio"><%= truncate(user.bio, length: 40) %></p>
        </div>
        </div>
      <% end %>
    </div>
</div>

