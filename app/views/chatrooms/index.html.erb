<ul>
<% @chatrooms.each do |chatroom| %>
<li>
    <div>
        <p><%= chatroom.user_one.username %></p>
        <p><%= chatroom.user_two.username %></p>
        <img src="<%= chatroom.user_one.profile_picture_url %>" alt="" class = "<%= current_user ? 'left' : 'right' %>">
        <img src="<%= chatroom.user_two.profile_picture_url  %>" alt="" class = "<%= current_user ? 'left' : 'right' %>">
        <%= link_to "go to messages ", chatroom_path(chatroom) %>
    </div></li>
<%=  %>
<% end %>
</ul>

<%= link_to 'back', :back %>